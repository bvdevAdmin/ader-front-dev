$(document).ready(function() {
	$("#tab > li").click(function() {
		let idx = $(this).index();
		
		switch(idx) {
			case 0: // 적립
			break;
			
			case 1: // 사용
			break;
		}

	}).eq(0).click();
});